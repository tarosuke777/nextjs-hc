version: "3.8" # Docker Compose のバージョンを指定。通常は最新の安定版でOK。

services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "11180:80"
    restart: always
    container_name: nextjs-hc
